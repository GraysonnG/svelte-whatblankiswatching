<style lang="scss">
  :global(body) {
    background-color: rgba(15,15,15);
    padding: 0;
    margin: 0;
  }

  :global(input),
  :global(select) {
    display: block;
    background: none;
    background-color: rgba(0, 0, 0, 0.4);
    border: none;
    color: white;
    padding: 0.7em;
    border-radius: 0.5em;
  }

  :global(option) {
    color: black;
  }

  :global(select) {
    appearance: none;
  }

  :global(label) {
    user-select: none;
  }
</style>
